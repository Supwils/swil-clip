[package]
name = "swil-clip"
version = "0.1.3"
description = "A minimal clipboard manager for macOS"
authors = ["supwilsoft"]
license = "MIT"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api", "tray-icon", "image-png"] }
tauri-plugin-log = "2"
tauri-plugin-store = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.22"

# At-rest encryption of clipboard history (key stored in the macOS Keychain).
aes-gcm = "0.10"

window-vibrancy = "0.6"

[target.'cfg(target_os = "macos")'.dependencies]
cocoa = "0.26"
objc = "0.2"
core-graphics = "0.24"
# 3.x shares core-foundation 0.10 with the rest of the tree (2.x would link a
# second core-foundation 0.9 into the binary).
security-framework = "3"
